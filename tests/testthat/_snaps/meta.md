# get_meta aborts on a frame carrying no metadata

    Code
      get_meta(cdisc_adsl)
    Condition
      Error:
      ! `x` carries no vport metadata.
      x No metadata_json attribute was found.
      i Run `apply_spec()` or `set_meta()` to attach metadata first.

