% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_construct.R
\name{is_vport_spec}
\alias{is_vport_spec}
\title{Test for a vport_spec object}
\usage{
is_vport_spec(x)
}
\arguments{
\item{x}{\emph{Object to test.} \verb{<any>}.}
}
\value{
\emph{A \verb{<logical(1)>}}: \code{TRUE} when \code{x} is a \code{vport_spec}, else \code{FALSE}.
}
\description{
Report whether an object is a \code{vport_spec} -- the validated CDISC
specification that drives the vport workflow (spec -> apply_spec ->
read_/write_). \code{\link[=vport_spec]{vport_spec()}} builds one; this is the type guard before you
pass it to \code{\link[=apply_spec]{apply_spec()}} or reach into it with the spec accessors.
}
\examples{
# ---- Example 1: guard a built specification ----
#
# vport_spec() assembles and validates a spec; is_vport_spec() confirms the
# type before you drive apply_spec() with it.
spec <- vport_spec(cdisc_datasets, cdisc_variables, codelists = cdisc_codelists)
is_vport_spec(spec)

# ---- Example 2: an ordinary object is not a spec ----
#
# Any non-vport_spec value -- a bare data frame, say -- returns FALSE.
is_vport_spec(cdisc_dm)

}
\seealso{
\code{\link[=vport_spec]{vport_spec()}} to build one; \code{\link[=is_vport_meta]{is_vport_meta()}} for the metadata
guard.
}
