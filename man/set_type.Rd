% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_type.R
\name{set_type}
\alias{set_type}
\title{Override a variable's dataType in a spec}
\usage{
set_type(spec, dataset, ...)
}
\arguments{
\item{spec}{\emph{The specification to amend.} \verb{<artoo_spec>: required}.}

\item{dataset}{\emph{The dataset whose variables to retype.} \verb{<character(1)>: required}. Must name a dataset in \code{spec}.}

\item{...}{\emph{Named \code{variable = type} pairs.} Each name is a variable in
\code{dataset}; each value is a CDISC \code{dataType} (\code{"string"}, \code{"integer"},
\code{"decimal"}, \code{"float"}, \code{"double"}, \code{"boolean"}, \code{"date"}, \code{"datetime"},
\code{"time"}, \code{"URI"}) or a recognised spelling of one. At least one pair is
required and every argument must be named.

\strong{Tip:} to undo an \code{integer} dataType that the data does not satisfy,
set \code{"float"} (IEEE double) or \code{"decimal"} (exact, exchanged as text).}
}
\value{
\emph{A new \verb{<artoo_spec>}} with the named variables retyped, ready for
\code{\link[=apply_spec]{apply_spec()}} or \code{\link[=write_spec]{write_spec()}}. The input \code{spec} is unchanged.
}
\description{
Return a new \code{artoo_spec} with one or more variables retyped. This is the
supported, in-R way to correct a spec when the data disagrees with its
declared \code{dataType} (e.g. a variable typed \code{integer} whose extract holds
fractional values): fix it here rather than editing the source workbook,
then drive \code{\link[=apply_spec]{apply_spec()}} with the corrected spec. The spec is immutable,
so the original is never changed.
}
\details{
\strong{Per-dataset scope.} A type is set only on the named \code{dataset}'s row.
A variable that appears in several datasets keeps its other rows' types;
call \code{set_type()} once per dataset to change them all. Spec-wide
consequences (a variable typed inconsistently across datasets) are a
\code{\link[=validate_spec]{validate_spec()}} concern, not a construction error here.

\strong{Canonicalised, then validated.} Each supplied type is mapped through
the closed CDISC \code{dataType} vocabulary, so \code{"Float"}, \code{"decimal"}, and
\code{"text"} all resolve; an unrecognised token aborts with
\code{artoo_error_type}. The rebuilt spec is re-validated, so an override that
would break the spec aborts with \code{artoo_error_spec}.
}
\examples{
# ---- Example 1: retype one variable the data disagrees with ----
#
# The bundled adam_spec types ADSL.AGE as integer. If an extract stored it
# with fractional values, retype it to float so apply_spec() coerces
# without loss. set_type() returns a new spec; the original is untouched.
fixed <- set_type(adam_spec, "ADSL", AGE = "float")
v <- spec_variables(fixed, "ADSL")
v[v$variable == "AGE", c("variable", "data_type")]

# ---- Example 2: retype several at once, original left intact ----
#
# Pass any number of variable = type pairs; canonical dataTypes and common
# spellings both resolve. The source spec is immutable, so adam_spec still
# reports AGE as its original type.
patched <- set_type(adam_spec, "ADSL", AGE = "decimal", TRTSDT = "date")
spec_variables(adam_spec, "ADSL")$data_type[
  spec_variables(adam_spec, "ADSL")$variable == "AGE"
]

}
\seealso{
\strong{Auto-repair:} \code{\link[=repair_spec]{repair_spec()}} to apply every \code{integer_fraction} /
\code{integer_overflow} fix from a findings frame at once.

\strong{Workflow:} \code{\link[=apply_spec]{apply_spec()}} to conform with the corrected spec;
\code{\link[=write_spec]{write_spec()}} to persist it; \code{\link[=check_spec]{check_spec()}} to find the mismatches.
}
