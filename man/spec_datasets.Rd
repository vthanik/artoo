% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_accessors.R
\name{spec_datasets}
\alias{spec_datasets}
\title{Dataset names in a spec}
\usage{
spec_datasets(spec)
}
\arguments{
\item{spec}{\emph{The specification to read.} \verb{<artoo_spec>: required}.}
}
\value{
\emph{A character vector of dataset names}, de-duplicated and with
\code{NA}s dropped. Empty when the spec has no datasets.
}
\description{
List the datasets a specification defines. The result is the set of
names you pass as the \code{dataset} argument to the other accessors and to
\code{apply_spec()}.
}
\examples{
# ---- Example 1: the datasets the pilot ADaM spec defines ----
#
# Build the spec from the bundled CDISC-pilot tables and list its
# datasets -- the names you pass to the other accessors.
spec <- artoo_spec(cdisc_datasets, cdisc_variables, codelists = cdisc_codelists)
spec_datasets(spec)

}
\seealso{
\code{\link[=spec_variables]{spec_variables()}} for one dataset's variables; \code{\link[=spec_keys]{spec_keys()}}
for its sort keys.
}
