% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_study.R
\name{check_study}
\alias{check_study}
\title{Check a whole study against its spec}
\usage{
check_study(
  spec,
  data,
  decode = c("none", "to_decode", "to_code"),
  checks = NULL
)
}
\arguments{
\item{spec}{\emph{The specification to check against.} \verb{<artoo_spec>: required}.}

\item{data}{\emph{The study's datasets.} \verb{<named list of data.frame>: required}. One entry per dataset, named by the dataset (e.g.
\code{list(ADSL = adsl, ADAE = adae)}). Every name must be a dataset in
\code{spec}.}

\item{decode}{\emph{Which codelist column to check against.} \verb{<character(1)>}.
Passed to \code{\link[=check_spec]{check_spec()}}; one of \code{"none"} (default), \code{"to_decode"},
\code{"to_code"}.}

\item{checks}{\emph{Which conformance dimensions to run.}
\verb{<artoo_checks> | NULL}. Passed to \code{\link[=check_spec]{check_spec()}}; \code{NULL} (default) runs
every dimension. Build a subset with \code{\link[=artoo_checks]{artoo_checks()}}.}
}
\value{
\emph{A \verb{<artoo_study_findings>} data frame} with the same columns as
\code{\link[=check_spec]{check_spec()}} (\code{check}, \code{dimension}, \code{severity}, \code{dataset}, \code{variable},
\code{message}), one row per divergence across all datasets. Zero rows means
the whole study conforms. Print it for the count matrix; treat it as an
ordinary data frame otherwise.
}
\description{
Run \code{\link[=check_spec]{check_spec()}} over every dataset in a study and return one stacked
findings frame. Where \code{\link[=check_spec]{check_spec()}} answers "does this dataset conform?",
\code{check_study()} answers "is my whole study submittable?" in a single pass,
surfacing every dataset's divergences at once instead of one abort at a
time. The result is an ordinary findings frame underneath, so filter it by
severity or hand it straight to \code{\link[=repair_spec]{repair_spec()}}.
}
\details{
\strong{One row per divergence, every dataset stacked.} Each dataset's findings
carry its name in the \code{dataset} column, so the frame is the union of the
per-dataset \code{\link[=check_spec]{check_spec()}} results. Printing renders the dataset-by-check
count matrix (the study-level summary); the underlying frame is unchanged.

\strong{Data-requiring, like \code{\link[=check_spec]{check_spec()}}.} \code{check_study()} checks data
against the spec, so it needs the data frames. For the spec's own
structural integrity (no data), use \code{\link[=validate_spec]{validate_spec()}}.
}
\examples{
# ---- Example 1: scan a whole study in one pass ----
#
# Loop the conformance check over every dataset's data. A fractional AGE
# (the spec types it integer) surfaces as an integer_fraction finding; the
# print is a dataset-by-check count matrix.
adsl <- cdisc_adsl
adsl$AGE <- adsl$AGE + 0.5
check_study(adam_spec, list(ADSL = adsl, ADAE = cdisc_adae))

# ---- Example 2: feed the findings straight into repair_spec() ----
#
# The result is an ordinary findings frame, so repair_spec() consumes it to
# flip every integer_fraction / integer_overflow variable across the study.
findings <- check_study(adam_spec, list(ADSL = adsl))
fixed <- repair_spec(adam_spec, findings)
spec_variables(fixed, "ADSL")$data_type[
  spec_variables(fixed, "ADSL")$variable == "AGE"
]

}
\seealso{
\strong{One dataset:} \code{\link[=check_spec]{check_spec()}}. \strong{Spec structure only:} \code{\link[=validate_spec]{validate_spec()}}.

\strong{Repair:} \code{\link[=repair_spec]{repair_spec()}} to apply the integer fixes the matrix surfaces.
}
