% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_accessors.R
\name{spec_comments}
\alias{spec_comments}
\title{Comment definitions in a spec}
\usage{
spec_comments(spec)
}
\arguments{
\item{spec}{\emph{The specification to read.} \verb{<artoo_spec>: required}.}
}
\value{
\emph{A data frame of comment metadata}, one row per comment, with all
four columns: \code{comment_id}, \code{description}, \code{document_id}, \code{pages}. Empty
when the spec defines no comments.
}
\description{
Return the comment definitions a specification carries. Datasets,
variables, value-level rows, and codelists reference these by
\code{comment_id}; \code{\link[=validate_spec]{validate_spec()}} checks the references resolve and each
referenced comment has a body.
}
\examples{
# ---- Example 1: the comments a spec defines ----
#
# Build a spec with one comment and read it back.
spec <- artoo_spec(
  data.frame(dataset = "ADSL"),
  data.frame(dataset = "ADSL", variable = "AGE", data_type = "integer"),
  comments = data.frame(
    comment_id = "C.AGE",
    description = "Age in years at informed consent.",
    stringsAsFactors = FALSE
  )
)
spec_comments(spec)

}
\seealso{
\code{\link[=spec_methods]{spec_methods()}}, \code{\link[=spec_documents]{spec_documents()}}, \code{\link[=validate_spec]{validate_spec()}}.
}
