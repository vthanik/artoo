% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_construct.R
\name{is_artoo_spec}
\alias{is_artoo_spec}
\title{Test for a artoo_spec object}
\usage{
is_artoo_spec(x)
}
\arguments{
\item{x}{\emph{Object to test.} \verb{<any>}.}
}
\value{
\emph{A \verb{<logical(1)>}}: \code{TRUE} when \code{x} is a \code{artoo_spec}, else \code{FALSE}.
}
\description{
Report whether an object is a \code{artoo_spec} -- the validated CDISC
specification that drives the artoo workflow (spec -> apply_spec ->
read_/write_). \code{\link[=artoo_spec]{artoo_spec()}} builds one; this is the type guard before you
pass it to \code{\link[=apply_spec]{apply_spec()}} or reach into it with the spec accessors.
}
\examples{
# ---- Example 1: guard a built specification ----
#
# artoo_spec() assembles and validates a spec; is_artoo_spec() confirms the
# type before you drive apply_spec() with it.
spec <- artoo_spec(cdisc_sdtm_datasets, cdisc_sdtm_variables, codelists = cdisc_codelists)
is_artoo_spec(spec)

# ---- Example 2: an ordinary object is not a spec ----
#
# Any non-artoo_spec value -- a bare data frame, say -- returns FALSE.
is_artoo_spec(cdisc_dm)

}
\seealso{
\code{\link[=artoo_spec]{artoo_spec()}} to build one; \code{\link[=is_artoo_meta]{is_artoo_meta()}} for the metadata
guard.
}
