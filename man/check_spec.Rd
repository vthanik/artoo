% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_spec.R
\name{check_spec}
\alias{check_spec}
\title{Check a dataset against its spec}
\usage{
check_spec(
  x,
  spec,
  dataset,
  decode = c("none", "to_decode", "to_code"),
  checks = NULL
)
}
\arguments{
\item{x}{\emph{The data frame to check.} \verb{<data.frame>: required}. Typically
the output of \code{\link[=apply_spec]{apply_spec()}}, but any frame works.}

\item{spec}{\emph{The specification to check against.} \verb{<artoo_spec>: required}.}

\item{dataset}{\emph{The dataset whose rules apply.} \verb{<character(1)>: required}.

\strong{Restriction:} must name a dataset in \code{spec} (see \code{\link[=spec_datasets]{spec_datasets()}}).}

\item{decode}{\emph{Which codelist column membership is checked against.}
\verb{<character(1)>}. One of \code{"none"} (default), \code{"to_decode"}, or
\code{"to_code"}.}

\item{checks}{\emph{Which conformance dimensions to evaluate.} \verb{<artoo_checks> | NULL}. When \code{NULL} (default) every dimension runs; build a control
with \code{\link[=artoo_checks]{artoo_checks()}} to disable some.}
}
\value{
\emph{A findings data frame} with columns \code{check}, \code{dimension},
\code{severity} (\code{"error"}, \code{"warning"}, or \code{"note"}), \code{dataset}, \code{variable},
and \code{message}, one row per divergence. Zero rows means the data conforms.
}
\description{
Compare a data frame to one dataset's specification and report where they
diverge. This is the data-conformance check at the end of the artoo
workflow (spec -> apply_spec -> check_spec): it reuses the metadata the
spec already carries (variables, types, lengths, codelists, keys). It is
distinct from \code{\link[=validate_spec]{validate_spec()}}, which checks the spec's own internal
integrity rather than the data. Both report findings keyed to the same open
rule catalog.
}
\details{
\strong{Findings, not enforcement.} \code{check_spec()} never modifies data; it
returns every divergence it finds. \code{\link[=apply_spec]{apply_spec()}} runs it and decides what
to do via its \code{conformance} argument (warn, abort, off). The dimensions
checked are: missing variables (split into mandatory, an error, and
permissible, a warning), extra variables (data column the spec does not
declare), type mismatch, ISO 8601 validity of character date/datetime/time
values (CDISC partials pass; \code{"12NOV2019"} does not), fractional values
and 32-bit overflow under an \code{integer} dataType (both would corrupt data
at coercion), character length overflow, the hard 200-byte XPORT v5 / FDA
character limit, codelist membership, label drift against the spec, key
uniqueness, and displayFormat validity.

\strong{Decode-aware membership.} \code{decode} selects which codelist column the
data is checked against, matching \code{\link[=apply_spec]{apply_spec()}}'s decode step:
\code{"none"}/\code{"to_code"} check against the codelist \code{term}s, \code{"to_decode"}
against the \code{decode}s. \code{\link[=apply_spec]{apply_spec()}} threads its own \code{decode} through, so
a decoded column is not wrongly flagged.
}
\examples{
# ---- Example 1: a conformed dataset has no findings ----
#
# apply_spec() scaffolds, coerces, and orders to spec; checking the result
# against the same spec returns zero rows.
adsl <- apply_spec(cdisc_adsl, adam_spec, "ADSL", conformance = "off")
nrow(check_spec(adsl, adam_spec, "ADSL"))

# ---- Example 2: raw data surfaces divergences ----
#
# Checking a raw frame with an undeclared column flags the extras.
raw <- cdisc_adsl
raw$NOTASPEC <- 1
head(check_spec(raw, adam_spec, "ADSL")[, c("check", "variable", "severity")])

}
\seealso{
\code{\link[=apply_spec]{apply_spec()}} which runs this; \code{\link[=artoo_checks]{artoo_checks()}} to select
dimensions; \code{\link[=validate_spec]{validate_spec()}} for spec integrity.
}
