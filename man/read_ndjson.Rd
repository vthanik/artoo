% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codec_ndjson.R
\name{read_ndjson}
\alias{read_ndjson}
\title{Read a dataset from CDISC Dataset-JSON NDJSON}
\usage{
read_ndjson(path, col_select = NULL, n_max = Inf)
}
\arguments{
\item{path}{\emph{Source \code{.ndjson} path.} \verb{<character(1)>: required}. A gzip
stream (\code{.ndjson.gz}) is inflated transparently. A file whose first line
is not the Dataset-JSON metadata object aborts with \code{vport_error_codec}.}

\item{col_select}{\emph{Variables to read.} \verb{<character> | NULL}. \code{NULL}
(default) reads every column; otherwise a vector of variable names.
Columns return in file order (not the requested order) and the
\code{vport_meta} is filtered to match. Works on every format: parquet narrows
columns natively, the rest filter after decode.

\strong{Note:} an unknown name is a \code{vport_error_input}, never a silent drop.}

\item{n_max}{\emph{Maximum records to read.} \verb{<numeric(1)>: default Inf}. Caps
the row count; the returned \code{vport_meta} reports the rows actually read.
xpt v8 bounds the disk read; the other formats cap after decode.}
}
\value{
\emph{A \verb{<data.frame>}} carrying \code{vport_meta} (read it with
\code{\link[=get_meta]{get_meta()}}).
}
\description{
Read a newline-delimited CDISC Dataset-JSON v1.1 (\code{.ndjson}) file back to
a data frame, restoring the full \code{vport_meta} from its metadata line and
realizing SAS date/datetime/time variables to R \code{Date} / \code{POSIXct} /
\code{vport_time}. Rows are parsed in bounded slabs, and \code{n_max} stops the
line loop early, so a partial read of a huge file never parses the tail.
A thin wrapper over \code{\link[=read_dataset]{read_dataset()}} with \code{format = "ndjson"}.
}
\examples{
spec <- vport_spec(cdisc_datasets, cdisc_variables, codelists = cdisc_codelists)

# ---- Example 1: round-trip a conformed dataset through NDJSON ----
#
# The variable labels, types, and keys survive the round-trip.
adsl <- apply_spec(cdisc_adsl, spec, "ADSL", conformance = "off")
path <- tempfile(fileext = ".ndjson")
write_ndjson(adsl, path)
back <- read_ndjson(path)
identical(get_meta(back)@columns, get_meta(adsl)@columns)

# ---- Example 2: a bounded partial read of the first rows ----
#
# n_max stops the line loop as soon as enough rows are in.
head_rows <- read_ndjson(path, n_max = 5)
get_meta(head_rows)@dataset$records

}
\seealso{
\code{\link[=write_ndjson]{write_ndjson()}} for the inverse; \code{\link[=read_json]{read_json()}} for the
array-form file; \code{\link[=read_dataset]{read_dataset()}} for the generic dispatcher.
}
