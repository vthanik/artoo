% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codec_xpt.R
\name{write_xpt}
\alias{write_xpt}
\title{Write a dataset to SAS XPORT}
\usage{
write_xpt(x, path, ...)
}
\arguments{
\item{x}{\emph{The dataset to write.} \verb{<data.frame>: required}. Typically the
output of \code{\link[=apply_spec]{apply_spec()}}, carrying \code{vport_meta}.}

\item{path}{\emph{Destination \code{.xpt} path.} \verb{<character(1)>: required}.}

\item{...}{\emph{Codec arguments} forwarded to the encoder:
\itemize{
\item \code{version} \verb{<integer(1)>: default 5}. \code{5} (FDA standard, names <= 8
chars and labels <= 40) or \code{8} (names <= 32, long labels).
\item \code{encoding} \verb{<character(1)> | NULL}. Target charset; \code{NULL} (default)
inherits the source encoding recorded in \code{vport_meta}, else UTF-8.
\item \code{on_invalid} \verb{<character(1)>: default "error"}. Policy for values not
representable in \code{encoding}: \code{"error"}, \code{"replace"}, or \code{"ignore"}.
\item \code{created} \verb{<POSIXct(1)>}. Timestamp stamped in the header; freeze it for
byte-stable output.
}}
}
\value{
\emph{The \code{path}}, invisibly.
}
\description{
Serialize a data frame to a SAS Transport (\code{.xpt}) file in v5 (the FDA
submission standard) or v8 (extended names and labels), preserving the
\code{vport_meta} a column can hold. The emit end of the vport workflow
(spec -> apply_spec -> write_xpt); a thin wrapper over \code{\link[=write_dataset]{write_dataset()}}
with \code{format = "xpt"}.
}
\details{
\strong{What XPORT can carry.} An \code{.xpt} file's NAMESTR stores only variable
name, label, length, and SAS format. CDISC metadata beyond that
(keySequence, codelist, origin, targetDataType, ...) and the source
encoding are not representable in the bytes; they ride the in-session
\code{vport_meta} and the sidecar in self-describing formats (Dataset-JSON,
Parquet, rds). XPORT also cannot distinguish an empty string from \code{NA}
(both store as blanks) and drops trailing spaces.
}
\examples{
spec <- vport_spec(cdisc_datasets, cdisc_variables, codelists = cdisc_codelists)

# ---- Example 1: write a conformed dataset as v5 (FDA standard) ----
#
# apply_spec() attaches the metadata; write_xpt() carries the label, length,
# and SAS format for each variable into the transport file.
adsl <- apply_spec(cdisc_adsl, spec, "ADSL", check = "off")
path <- tempfile(fileext = ".xpt")
write_xpt(adsl, path)

# ---- Example 2: v8 for long names, with a frozen timestamp ----
#
# Version 8 keeps names over 8 characters; a fixed `created` makes the bytes
# reproducible.
dm <- apply_spec(cdisc_dm, spec, "DM", check = "off")
path8 <- tempfile(fileext = ".xpt")
write_xpt(dm, path8, version = 8, created = as.POSIXct("2020-01-01", tz = "UTC"))

}
\seealso{
\code{\link[=read_xpt]{read_xpt()}} for the inverse; \code{\link[=write_dataset]{write_dataset()}} for the generic
dispatcher.
}
