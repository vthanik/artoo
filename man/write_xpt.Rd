% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codec_xpt.R
\name{write_xpt}
\alias{write_xpt}
\title{Write a dataset to SAS XPORT}
\usage{
write_xpt(
  x,
  path,
  version = 5,
  encoding = NULL,
  on_invalid = c("error", "replace", "ignore"),
  created = NULL
)
}
\arguments{
\item{x}{\emph{The dataset to write.} \verb{<data.frame>: required}. Typically the
output of \code{\link[=apply_spec]{apply_spec()}}, carrying \code{artoo_meta}.}

\item{path}{\emph{Destination \code{.xpt} path.} \verb{<character(1)>: required}.}

\item{version}{\emph{XPORT transport version.} \verb{<integer(1)>: default 5}. \code{5}
(the FDA standard: names <= 8 characters, labels <= 40 bytes) or \code{8}
(names <= 32, long labels).}

\item{encoding}{\emph{Target charset.} \verb{<character(1)> | NULL}. \code{NULL}
(default) inherits the source encoding recorded in \code{artoo_meta}, else
UTF-8. IANA and SAS names (\code{"US-ASCII"}, \code{"wlatin1"}) both work.

\strong{Tip:} any SAS or IANA spelling listed by \code{\link[=artoo_encodings]{artoo_encodings()}} is
accepted.}

\item{on_invalid}{\emph{Policy for values not representable in \code{encoding}.}
\verb{<character(1)>: default "error"}. One of \code{"error"} (abort with
\code{artoo_error_codec}, naming the offenders), \code{"replace"} (substitute
\verb{?} and warn with \code{artoo_warning_encoding}), or \code{"ignore"} (drop
them). The same policy vocabulary as the UTF-8 writers
(\code{\link[=write_json]{write_json()}}, \code{\link[=write_ndjson]{write_ndjson()}}, \code{\link[=write_parquet]{write_parquet()}}).}

\item{created}{\emph{Header timestamp.} \verb{<POSIXct(1)> | NULL}. \code{NULL} (default)
stamps the current time; freeze it for byte-stable output.}
}
\value{
\emph{The input \code{x}}, invisibly, so a write can sit mid-pipeline.
}
\description{
Serialize a data frame to a SAS Transport (\code{.xpt}) file in v5 (the FDA
submission standard) or v8 (extended names and labels), preserving the
\code{artoo_meta} a column can hold. The emit end of the artoo workflow
(spec -> apply_spec -> write_xpt); a thin wrapper over \code{\link[=write_dataset]{write_dataset()}}
with \code{format = "xpt"}.
}
\details{
\strong{What XPORT can carry.} An \code{.xpt} file's NAMESTR stores only variable
name, label, length, and SAS format. CDISC metadata beyond that
(keySequence, codelist, origin, targetDataType, ...) and the source
encoding are not representable in the bytes; they ride the in-session
\code{artoo_meta} and the sidecar in self-describing formats (Dataset-JSON,
Parquet, rds). XPORT also cannot distinguish an empty string from \code{NA}
(both store as blanks) and drops trailing spaces.

\strong{Character ISO dates (\code{--DTC}) write as text.} A character column whose
\code{dataType} is \code{date}/\code{datetime}/\code{time} with no numeric \code{targetDataType} is
the CDISC ISO 8601 text form — the SDTM \code{--DTC} convention — and stores
as a character variable, partial dates (\code{"1951"}, \code{"1951-12"}) included,
byte for byte. The SAS-numeric encoding (with \code{DATE9.}-style formats) is
used for columns that are R \code{Date}/\code{POSIXct}/\code{hms} or whose
metadata records \code{targetDataType = "integer"} (the ADaM numeric-date
convention). A character column \emph{under} \code{targetDataType = "integer"}
aborts loudly — a partial date can never become a SAS numeric silently.
}
\examples{
spec <- artoo_spec(
  cdisc_adam_datasets, cdisc_adam_variables,
  codelists = cdisc_codelists
)

# ---- Example 1: write a conformed dataset as v5 (FDA standard) ----
#
# apply_spec() attaches the metadata; write_xpt() carries the label, length,
# and SAS format for each variable into the transport file.
adsl <- apply_spec(cdisc_adsl, spec, "ADSL", conformance = "off")
path <- tempfile(fileext = ".xpt")
write_xpt(adsl, path)

# ---- Example 2: v8 for long names, with a frozen timestamp ----
#
# Version 8 keeps names over 8 characters; a fixed `created` makes the bytes
# reproducible. Reading it back shows the labels, types, and record count
# survived the transport. DM is SDTM, so it conforms against the bundled
# sdtm_spec.
dm <- apply_spec(cdisc_dm, sdtm_spec, "DM", conformance = "off")
path8 <- tempfile(fileext = ".xpt")
write_xpt(dm, path8, version = 8, created = as.POSIXct("2020-01-01", tz = "UTC"))
get_meta(read_xpt(path8))@dataset$records

}
\seealso{
\code{\link[=read_xpt]{read_xpt()}} for the inverse; \code{\link[=write_dataset]{write_dataset()}} for the generic
dispatcher.
}
