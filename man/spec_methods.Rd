% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_accessors.R
\name{spec_methods}
\alias{spec_methods}
\title{Derivation methods in a spec}
\usage{
spec_methods(spec)
}
\arguments{
\item{spec}{\emph{The specification to read.} \verb{<vport_spec>: required}.}
}
\value{
\emph{A data frame of method metadata} (\code{method_id}, \code{name}, \code{type},
\code{description}, ...), one row per method. Empty when the spec defines no
methods.
}
\description{
Return the method definitions a specification carries. Variables and
value-level rows reference these by \code{method_id}; \code{\link[=validate_spec]{validate_spec()}} checks
that every reference resolves and that each referenced method is
complete (has a description).
}
\examples{
# ---- Example 1: the methods a spec defines ----
#
# Build a spec with one derivation method and read it back.
spec <- vport_spec(
  data.frame(dataset = "ADSL"),
  data.frame(dataset = "ADSL", variable = "AGEGR1", data_type = "string"),
  methods = data.frame(
    method_id = "MT.AGEGR1",
    description = "Age group from AGE.",
    stringsAsFactors = FALSE
  )
)
spec_methods(spec)

}
\seealso{
\code{\link[=spec_comments]{spec_comments()}}, \code{\link[=spec_documents]{spec_documents()}}, \code{\link[=validate_spec]{validate_spec()}}.
}
