% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codec_json.R
\name{read_json}
\alias{read_json}
\title{Read a dataset from CDISC Dataset-JSON}
\usage{
read_json(path, col_select = NULL, n_max = Inf, encoding = NULL)
}
\arguments{
\item{path}{\emph{Source \code{.json} path.} \verb{<character(1)>: required}. A JSON file
that is not Dataset-JSON v1.1 aborts with \code{artoo_error_codec}.}

\item{col_select}{\emph{Variables to read.} \verb{<character> | NULL}. \code{NULL}
(default) reads every column; otherwise a vector of variable names.
Columns return in file order (not the requested order) and the
\code{artoo_meta} is filtered to match. Works on every format: parquet narrows
columns natively, the rest filter after decode.

\strong{Note:} an unknown name is a \code{artoo_error_input}, never a silent drop.}

\item{n_max}{\emph{Maximum records to read.} \verb{<numeric(1)>: default Inf}. Caps
the row count; the returned \code{artoo_meta} reports the rows actually read.
xpt v8 bounds the disk read; the other formats cap after decode.}

\item{encoding}{\emph{Source charset of the file bytes.} \verb{<character(1)> | NULL}. \code{NULL} (default) reads UTF-8, as Dataset-JSON requires. Pass an
IANA or SAS charset name (e.g. \code{"windows-1252"}) only to read a
non-conformant file a producer wrote in that charset; the bytes are
transcoded to UTF-8 on read.}
}
\value{
\emph{A \verb{<data.frame>}} carrying \code{artoo_meta} (read it with
\code{\link[=get_meta]{get_meta()}}).
}
\description{
Read a CDISC Dataset-JSON v1.1 (\code{.json}) file back to a data frame,
restoring the full \code{artoo_meta} it carries and realizing SAS
date/datetime/time variables to R \code{Date} / \code{POSIXct} / \code{artoo_time}. Column
types are reconstructed from the recorded metadata, not guessed from the
JSON tokens, so the round-trip is lossless. The ingest end of the I/O layer;
a thin wrapper over \code{\link[=read_dataset]{read_dataset()}} with \code{format = "json"}.
}
\examples{
spec <- artoo_spec(cdisc_datasets, cdisc_variables, codelists = cdisc_codelists)

# ---- Example 1: round-trip a conformed dataset through Dataset-JSON ----
#
# The variable labels, types, and keys survive the round-trip.
adsl <- apply_spec(cdisc_adsl, spec, "ADSL", conformance = "off")
path <- tempfile(fileext = ".json")
write_json(adsl, path)
back <- read_json(path)
identical(get_meta(back)@columns, get_meta(adsl)@columns)

# ---- Example 2: the metadata names the dataset and row count ----
#
# The restored artoo_meta exposes the dataset-level attributes.
get_meta(back)@dataset$records

}
\seealso{
\code{\link[=write_json]{write_json()}} for the inverse; \code{\link[=read_dataset]{read_dataset()}} for the generic
dispatcher.
}
