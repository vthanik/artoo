% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_accessors.R
\name{spec_study}
\alias{spec_study}
\title{Study-level metadata}
\usage{
spec_study(spec, field = NULL)
}
\arguments{
\item{spec}{\emph{The specification to read.} \verb{<artoo_spec>: required}.}

\item{field}{\emph{Return one field instead of the row.} \verb{<character(1)> | NULL}. When \code{NULL} (default) the whole study data frame is returned.

\strong{Restriction:} a non-\code{NULL} \code{field} must be a column of the study
table; an unknown field aborts with \code{artoo_error_input}.}
}
\value{
\emph{The study data frame} (one row), or the value of one \code{field}.
The canonical fields are \code{study_name}, \code{study_description}, and
\code{protocol_name} — every source spelling is canonicalised to these by
\code{\link[=artoo_spec]{artoo_spec()}} — plus any other field the source carried verbatim
(e.g. \code{define_version} from a Define-XML read).
}
\description{
Return the study-level metadata row, or a single field from it. Holds
the canonical study fields (\code{study_name}, \code{study_description},
\code{protocol_name} — every source spelling is canonicalised by
\code{\link[=artoo_spec]{artoo_spec()}}) plus any other study-scoped fields a source provides
(the CDISC standard lives on its own property — see \code{\link[=spec_standard]{spec_standard()}}).
}
\examples{
# ---- Example 1: the whole study row, then one field ----
#
# spec_study() with no field returns the study-level table; pass a field
# name to pull a single value such as the study name.
spec <- artoo_spec(
  cdisc_adam_datasets, cdisc_adam_variables,
  codelists = cdisc_codelists,
  study = data.frame(study_name = "CDISCPILOT01")
)
spec_study(spec)
spec_study(spec, "study_name")

}
\seealso{
\code{\link[=spec_datasets]{spec_datasets()}} for the datasets the study scopes;
\code{\link[=spec_standard]{spec_standard()}} for the spec's CDISC standard.
}
