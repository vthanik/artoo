% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vport_time.R
\name{vport_time}
\alias{vport_time}
\title{Build a clock-time-of-day value}
\usage{
vport_time(x = double())
}
\arguments{
\item{x}{\emph{Seconds since midnight.} \verb{<numeric>: default empty}. Coerced to
double. May exceed 86400 (elapsed times past 24h) or be negative; both
round-trip losslessly. Fractional seconds are kept and render hms-style
(\code{08:30:00.5}). \code{NA} is preserved.}
}
\value{
\emph{A \verb{<vport_time>}} of the same length as \code{x}.
}
\description{
Construct a \code{vport_time}: vport's class for SAS \code{TIME} values, held as
seconds since midnight and rendered \code{HH:MM:SS} in the RStudio/Positron
data viewer. \code{read_xpt()} and \code{\link[=apply_spec]{apply_spec()}} produce these for time
variables; build one directly to assemble a time column by hand. Chosen
over \code{hms} to keep vport free of the \code{vctrs} dependency.
}
\examples{
# ---- Example 1: build and render a few clock times ----
#
# Seconds since midnight become HH:MM:SS in print and in the data viewer.
vport_time(c(0, 30600, 86399))

# ---- Example 2: a time column inside a data frame ----
#
# vport_time survives data.frame() as a column and keeps its clock display.
data.frame(visit = c("BASELINE", "WEEK 2"), aval_tm = vport_time(c(28800, 50400)))

}
\seealso{
\code{\link[=is_vport_time]{is_vport_time()}} to test; \code{\link[=apply_spec]{apply_spec()}} which realizes SAS time
variables to this class.
}
