% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repair_spec.R
\name{repair_spec}
\alias{repair_spec}
\title{Repair a spec from its conformance findings}
\usage{
repair_spec(spec, findings)
}
\arguments{
\item{spec}{\emph{The specification to repair.} \verb{<artoo_spec>: required}.}

\item{findings}{\emph{A findings data frame.} \verb{<data.frame>: required}. The
result of \code{\link[=check_spec]{check_spec()}} or \code{check_study()}; must carry the \code{check},
\code{dataset}, and \code{variable} columns.}
}
\value{
\emph{A new \verb{<artoo_spec>}} with the flagged variables retyped to
\code{"float"}, or \code{spec} unchanged when there is nothing to repair. The input
is never mutated.
}
\description{
Take the \code{integer_fraction} and \code{integer_overflow} findings a
\code{\link[=check_spec]{check_spec()}} or \code{check_study()} run reports and return a new spec with
every offending variable retyped to \code{"float"}, so a frame that the
original spec would refuse to coerce now conforms. This closes the loop on
the spec-side fix: inspect the findings, then apply them all at once
instead of editing the source workbook variable by variable. Persist the
result with \code{\link[=write_spec]{write_spec()}}.
}
\details{
\strong{Scope.} Only the two lossy-integer findings are repaired
(\code{integer_fraction}, \code{integer_overflow}) — both mean "the spec says
\code{integer} but the data is not", and \code{"float"} is the loss-free fix. Other
findings are ignored; this is not a general spec rewriter. When no
repairable finding is present the spec is returned unchanged, with a note.

\strong{Built on \code{\link[=set_type]{set_type()}}.} Each \verb{(dataset, variable)} pair is applied
through the same validated override \code{\link[=set_type]{set_type()}} uses, so the result is a
fully re-validated \code{artoo_spec}, never a hand-edited internal.
}
\examples{
# ---- Example 1: auto-repair an integer/fractional mismatch ----
#
# adam_spec types ADSL.AGE as integer. Give it fractional ages and
# check_spec() raises an integer_fraction error; repair_spec() flips AGE
# (and only AGE) to float, and the corrected spec then applies cleanly.
dat <- cdisc_adsl
dat$AGE <- dat$AGE + 0.5
findings <- check_spec(dat, adam_spec, "ADSL")
fixed <- repair_spec(adam_spec, findings)
spec_variables(fixed, "ADSL")$data_type[
  spec_variables(fixed, "ADSL")$variable == "AGE"
]

# ---- Example 2: nothing to repair is a no-op ----
#
# The bundled data conforms, so its findings carry no integer_fraction or
# integer_overflow rows and the spec is returned unchanged.
clean <- check_spec(cdisc_adsl, adam_spec, "ADSL")
identical(repair_spec(adam_spec, clean), adam_spec)

}
\seealso{
\strong{Primitive:} \code{\link[=set_type]{set_type()}} to retype a chosen variable directly.

\strong{Findings:} \code{\link[=check_spec]{check_spec()}} for one dataset, \code{check_study()} across a
study. \strong{Persist:} \code{\link[=write_spec]{write_spec()}}.
}
