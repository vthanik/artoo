% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_accessors.R
\name{spec_keys}
\alias{spec_keys}
\title{Sort keys for a dataset}
\usage{
spec_keys(spec, dataset)
}
\arguments{
\item{spec}{\emph{The specification to read.} \verb{<vport_spec>: required}.}

\item{dataset}{\emph{The dataset whose keys to parse.} \verb{<character(1)>: required}.

\strong{Restriction:} must name a dataset in the spec.}
}
\value{
\emph{A character vector of key variable names}, split from the
dataset's \code{keys} cell (whitespace- or comma-separated). Empty when no
keys are declared.
}
\description{
Parse a dataset's sort keys into a character vector of variable names.
These keys drive the sort step of \code{apply_spec()} and the \code{keySequence}
written to each output format.
}
\examples{
# ---- Example 1: parse a dataset's sort keys ----
#
# Declare DM's keys, then read them back as the ordered vector apply_spec()
# sorts by. (STUDYID and USUBJID are real DM variables in the demo data.)
ds <- cdisc_datasets
ds$keys[ds$dataset == "DM"] <- "STUDYID USUBJID"
spec <- vport_spec(ds, cdisc_variables, codelists = cdisc_codelists)
spec_keys(spec, "DM")

}
\seealso{
\code{\link[=spec_datasets]{spec_datasets()}} for the dataset names; \code{\link[=spec_variables]{spec_variables()}} for
the variables a key must reference.
}
