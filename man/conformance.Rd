% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformance.R
\name{conformance}
\alias{conformance}
\title{Read the conformance findings a dataset carries}
\usage{
conformance(x)
}
\arguments{
\item{x}{\emph{A data frame produced by \code{\link[=apply_spec]{apply_spec()}}.} \verb{<data.frame>: required}.

\strong{Requirement:} the conformance check must have run: a frame from
\code{apply_spec(..., conformance = "off")} (or one rebuilt by a transform
that dropped attributes) carries no findings and aborts with
\code{artoo_error_input}.}
}
\value{
\emph{A \verb{<artoo_findings>} data frame} with columns \code{check},
\code{dimension}, \code{severity} (\code{"error"}, \code{"warning"}, or \code{"note"}),
\code{dataset}, \code{variable}, and \code{message}. Zero rows means the data
conformed. Print it for the sectioned report; treat it as an ordinary
data frame for programmatic use.
}
\description{
Pull the conformance findings \code{\link[=apply_spec]{apply_spec()}} attached to a conformed
data frame — the readable answer to "what did the check find?". The
result is the same findings frame \code{\link[=check_spec]{check_spec()}} returns (one row per
divergence), with a print method that renders a sectioned report, so
\code{conformance(adsl)} at the console is the inspection step the
\code{artoo_warning_conformance} warning points you at.
}
\examples{
spec <- artoo_spec(
  cdisc_sdtm_datasets, cdisc_sdtm_variables,
  codelists = cdisc_codelists
)

# ---- Example 1: inspect what the conform step found ----
#
# Conforming raw DM records the findings on the result; conformance()
# renders them as a report instead of a raw attribute.
dm <- suppressWarnings(apply_spec(cdisc_dm, spec, "DM"))
conformance(dm)

# ---- Example 2: gate a pipeline on error-severity findings ----
#
# The findings frame is an ordinary data frame: filter by severity to
# drive your own logic.
f <- conformance(dm)
nrow(f[f$severity == "error", ])

}
\seealso{
\code{\link[=apply_spec]{apply_spec()}} which attaches the findings; \code{\link[=check_spec]{check_spec()}} for
the same check on demand; \code{\link[=artoo_checks]{artoo_checks()}} to select dimensions.
}
