% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/columns.R
\name{columns}
\alias{columns}
\title{View a dataset's variable attributes, SAS-style}
\usage{
columns(x, member = NULL)
}
\arguments{
\item{x}{\emph{What to describe.} \verb{<data.frame> | <character(1)>: required}.
A stamped frame (carries \code{artoo_meta}), any plain data frame, or a
path to a dataset file (\code{.xpt}, \code{.json}, \code{.ndjson}, \code{.parquet},
\code{.rds}).}

\item{member}{\emph{XPORT member to describe.} \verb{<character(1)> | NULL}. Only
meaningful when \code{x} is a path to a multi-member \code{.xpt} file.}
}
\value{
\emph{A \verb{<artoo_columns>} data frame} with columns \verb{#}, \code{Variable},
\code{Type}, \code{Len}, \code{Format}, \code{Informat}, \code{Label}, \code{Key}, printed
left-aligned. It is an ordinary data frame underneath -- filter or
inspect it like one.
}
\description{
Return a one-row-per-variable attribute table -- the pane a SAS
programmer reads in \verb{PROC CONTENTS} or the Universal Viewer: position,
name, Char/Num type, length, format, informat, label, and the CDISC key
sequence. This is the quick look after \code{\link[=apply_spec]{apply_spec()}} stamps a frame, or
on any dataset file artoo can read.
}
\details{
\strong{Every real column shows.} The table covers the \emph{frame's} columns: a
column the spec never declared (which \code{\link[=apply_spec]{apply_spec()}} keeps, never drops)
still appears, its attributes inferred from the R class. A plain,
never-stamped data frame works the same way -- every attribute is
inferred.

\strong{A path reads through the codec.} A file path is dispatched by
extension through the same registry as \code{\link[=read_dataset]{read_dataset()}}, so the
attributes come from the one lossless reader (an unknown extension
aborts with the registry's known-extensions message).

\strong{Tip:} a multi-member XPORT file needs \verb{member =}; without one the
xpt reader aborts and points at \code{\link[=xpt_members]{xpt_members()}} for the listing.

\strong{Note:} an \code{.xpt} path shows a blank \code{Key}: the XPORT byte layout
stores only name, label, length, and formats, so \code{keySequence} (like
codelist and origin) cannot ride in the file. The metadata-carrying
formats (\code{.json}, \code{.ndjson}, \code{.parquet}, \code{.rds}) and the in-session
conformed frame show it; re-apply the spec after an xpt read to
restore it.
}
\examples{
spec <- artoo_spec(cdisc_datasets, cdisc_variables, codelists = cdisc_codelists)

# ---- Example 1: the column pane of a conformed frame ----
#
# apply_spec() stamps ADSL with its metadata; columns() reads it back as
# the SAS-style attribute table.
adsl <- apply_spec(cdisc_adsl, spec, "ADSL", conformance = "off")
columns(adsl)

# ---- Example 2: straight off a file ----
#
# Write the conformed frame to any format and point columns() at the
# path; the codec reads it back and the attributes are identical.
p <- tempfile(fileext = ".json")
write_json(adsl, p)
columns(p)

}
\seealso{
\strong{Members:} \code{\link[=xpt_members]{xpt_members()}} lists a multi-member XPORT file.

\strong{Metadata:} \code{\link[=get_meta]{get_meta()}} for the full \code{artoo_meta}; \code{\link[=apply_spec]{apply_spec()}}
which stamps it.
}
