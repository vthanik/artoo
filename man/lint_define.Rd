% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lint_define.R
\name{lint_define}
\alias{lint_define}
\title{Check the reference integrity of a Define-XML document}
\usage{
lint_define(path)
}
\arguments{
\item{path}{\emph{Define-XML document to check.} \verb{<character(1)>: required}.}
}
\value{
\emph{An \code{artoo_check} object.} Its \verb{@findings} data frame has columns
\code{check}, \code{dimension}, \code{severity}, \code{dataset}, \code{variable}, \code{message}, and is
empty when every reference resolves and every definition is used.
}
\description{
Reports OID references that resolve to nothing, and definitions that nothing
references. Neither is expressible in XML Schema, so a document can pass
\code{\link[=validate_define]{validate_define()}} and still fail here.
}
\details{
\strong{Findings are directional.} A dangling reference and an orphan definition
have different causes and different consequences, so they are separate
conditions. Dangling references are errors. Orphans are warnings, except for
an orphaned value list: a \code{def:ValueListDef} that no \code{def:ValueListRef}
points at means every value-level definition it holds renders nowhere, which
is a silent loss of submission metadata rather than untidiness.

\strong{Two exemptions prevent false positives.} A \code{CodeList} backed by an
\code{ExternalCodeList} (MedDRA, WHODrug, ISO 3166) is a dictionary reference and
is not expected to be referenced by a \code{CodeListRef}. A \code{CodeList} reached
through \verb{ItemRef/@RoleCodeListOID} counts as referenced. Without these,
every real adverse-event or medication define reports spurious orphans.
}
\examples{
# ---- Example 1: a sound document ----
#
# The bundled minimal example resolves cleanly, so the findings table is
# empty and the summary counts what was inspected.
minimal <- system.file("extdata", "define-minimal.xml", package = "artoo")
report <- lint_define(minimal)
nrow(report@findings)
report@summary$n_definitions

# ---- Example 2: a reference that resolves to nothing ----
#
# Point a variable's codelist reference at an OID no CodeList defines. The
# document still passes schema validation; only the lint sees it.
broken <- tempfile(fileext = ".xml")
writeLines(
  sub('CodeListOID="CL.SEX"', 'CodeListOID="CL.MISSING"',
    readLines(minimal),
    fixed = TRUE
  ),
  broken
)
validate_define(broken)@summary$valid
lint_define(broken)@findings[, c("check", "severity", "message")]

}
\seealso{
\strong{Validate first:} \code{\link[=validate_define]{validate_define()}} for schema conformance, which this
complements rather than repeats.
}
