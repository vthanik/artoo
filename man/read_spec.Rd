% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_read.R
\name{read_spec}
\alias{read_spec}
\title{Read a specification from JSON or Excel}
\usage{
read_spec(path)
}
\arguments{
\item{path}{\emph{The specification file to read.} \verb{<character(1)>: required}. A \code{.json} (native) or \code{.xlsx} / \code{.xls} (P21) file.

\strong{Requirement:} reading a P21 workbook needs the \code{readxl} package.}
}
\value{
\emph{A validated \code{vport_spec}.} Inspect it with \code{\link[=spec_datasets]{spec_datasets()}} /
\code{\link[=spec_variables]{spec_variables()}}, check it with \code{\link[=validate_spec]{validate_spec()}}, or persist it
with \code{\link[=write_spec]{write_spec()}}.
}
\description{
Read a clinical-dataset specification into a validated \code{vport_spec},
dispatching on the file extension: vport's native JSON (the inverse of
\code{\link[=write_spec]{write_spec()}}) or a Pinnacle 21 (P21) Define-XML Excel workbook. The
returned spec is the lingua franca the rest of vport applies and
serialises.
}
\details{
\strong{Two formats, one validator.} A \code{.json} file is read as vport native
JSON; a \code{.xlsx} / \code{.xls} file is read as a P21 workbook. Either way the
result is built through \code{\link[=vport_spec]{vport_spec()}}, so type canonicalisation and
cross-slot integrity checks are identical regardless of source.

\strong{P21 ingestion.} Sheets are located by a tolerant alias match
(case-, space-, and spelling-variant insensitive). Datasets and
Variables are required; Codelists and ValueLevel are optional (the
latter becomes the spec's value-level slot). Every cell is read as
text, then the dataset and codelist foreign keys are forward-filled to
recover merged cells (which the Excel reader returns as \code{NA} on
continuation rows). A key that cannot be resolved aborts with
\code{vport_error_spec} rather than being silently dropped.
}
\examples{
# ---- Example 1: round-trip a spec through native JSON ----
#
# write_spec() and read_spec() are inverses on the JSON path: the spec
# that comes back is identical to the one written.
spec <- vport_spec(cdisc_datasets, cdisc_variables, codelists = cdisc_codelists)
path <- tempfile(fileext = ".json")
write_spec(spec, path)
back <- read_spec(path)
identical(back, spec)

# ---- Example 2: read back, then inspect a dataset's variables ----
#
# A spec read from disk behaves exactly like one built in memory -- the
# accessors work unchanged.
head(spec_variables(back, "DM")[, c("variable", "label", "data_type")])

}
\seealso{
\strong{Inverse:} \code{\link[=write_spec]{write_spec()}} serialises a spec to native JSON.

\strong{Build / inspect:} \code{\link[=vport_spec]{vport_spec()}}, \code{\link[=spec_datasets]{spec_datasets()}},
\code{\link[=spec_variables]{spec_variables()}}, \code{\link[=validate_spec]{validate_spec()}}.
}
