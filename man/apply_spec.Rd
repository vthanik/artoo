% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply.R
\name{apply_spec}
\alias{apply_spec}
\title{Conform a data frame to its spec}
\usage{
apply_spec(
  x,
  spec,
  dataset,
  conformance = c("warn", "abort", "off"),
  na_position = c("first", "last"),
  extra = c("keep", "drop")
)
}
\arguments{
\item{x}{\emph{The raw data frame to conform.} \verb{<data.frame>: required}.}

\item{spec}{\emph{The specification to conform to.} \verb{<artoo_spec>: required}.}

\item{dataset}{\emph{The dataset whose rules apply.} \verb{<character(1)>: required}. Must name a dataset in \code{spec}.}

\item{conformance}{\emph{What to do with conformance findings.}
\verb{<character(1)>}. One of:
\itemize{
\item \code{"warn"} (default) run \code{\link[=check_spec]{check_spec()}}, attach the findings (read
them with \code{\link[=conformance]{conformance()}}), warn on any error-severity finding.
\item \code{"abort"} abort with \code{artoo_error_conformance} on any error-severity
finding.
\item \code{"off"} skip the check entirely.
}

\strong{Note:} this governs only the \emph{findings} disposition — what is
\emph{reported}. Pipeline errors are a different category and abort under
every setting, including \code{"off"}: an unknown dataset, and above all
lossy coercion (\code{artoo_error_type}), which no \code{conformance} value
bypasses. If the abort names variables whose spec dataType is
\code{integer} but whose data carries fractions, the fix is the spec
(retype to \code{"float"}/\code{"decimal"}), not this argument; the condition's
\verb{$variables} frame lists every offender.}

\item{na_position}{\emph{Where missing key values sort.} \verb{<character(1)>}. One
of \code{"first"} (default) or \code{"last"}. \code{"first"} matches SAS \verb{PROC SORT}
(and the FDA submission convention) by ordering missings before present
values; \code{"last"} matches R's \code{order()} and the pandas/Polars default.
Both are lossless; pick the one your comparison target uses.}

\item{extra}{\emph{What happens to undeclared columns.} \verb{<character(1)>}.
An "extra" is a column of \code{x} the spec does not declare — typically a
derivation temporary. One of:
\itemize{
\item \code{"keep"} (default) extras ride along after the declared columns,
reported by the \code{extra_variable} finding.
\item \code{"drop"} the returned frame carries exactly the spec's columns;
the drop is announced (\code{artoo_message_apply}), and that message is
the audit trail of what was removed.
}

\strong{Interaction:} the drop runs \emph{before} the check, so \code{\link[=conformance]{conformance()}}
reports only the columns the returned frame keeps. Under
\code{conformance = "abort"} an error-severity finding still aborts (those
findings arise only on spec-declared columns, which the drop never
touches) and the input is never mutated, so the trim cannot mask a
failure.

\strong{Note:} \code{"keep"} is the default deliberately. artoo is a lossless
carrier, so the metadata step never silently discards a column;
extras are surfaced every run (the \code{extra_variable} finding, and a
warning under \code{conformance = "warn"}), making \code{"drop"} a conscious opt-in.}
}
\value{
\emph{A conformed \verb{<data.frame>}} carrying \code{artoo_meta} (read it with
\code{\link[=get_meta]{get_meta()}}) and, unless \code{conformance = "off"}, the findings frame
\code{\link[=conformance]{conformance()}} reads back. Hand it to any \verb{write_*()} codec.
}
\description{
Run the ordered, transactional artoo pipeline that turns a raw analysis
data frame into one conformed to its specification and carrying
\code{artoo_meta}. This is the middle of the workflow (spec -> apply_spec ->
read_/write_): the conformed frame is ready for any \verb{write_*()} codec, and
the metadata it now carries makes that write lossless. The input is never
mutated; if any step aborts, the call leaves your data untouched.
}
\details{
\strong{Ordered pipeline.} Four fixed steps run in order: coerce each column
to its CDISC dataType, reorder columns to the spec, sort rows by the
dataset keys, then stamp the metadata. A spec variable the data lacks is
never fabricated as an empty column: artoo is a lossless carrier, not a
deriver. It is reported instead, an informational heads-up at apply time
plus a \code{missing_variable} finding (when mandatory) or \code{missing_permissible}
(when not), and left absent, so the conformed frame carries only the
columns the data actually had.

\strong{Extras are kept by default.} A column the spec does not declare
survives the pipeline (ordered after the declared ones), is \emph{reported}
by the \code{extra_variable} conformance finding, and round-trips through
every \verb{write_*()} codec with metadata inferred from its R class —
membership reported, never enforced by silent destruction. Keeping is
the default because artoo is lossless by construction: a
metadata-application step that silently discarded columns would break
that contract, so trimming data is always an explicit, announced choice
rather than a default side effect.
\code{extra = "drop"} opts in to trim-to-spec (the returned frame carries
exactly the spec's columns): the undeclared columns are removed \emph{before}
the check, so the findings describe exactly the returned frame (a dropped
column is never reported as \code{extra_variable}), and the drop itself is
always announced (\code{artoo_message_apply}) as the audit trail of what was
removed — even under \code{conformance = "off"}.

\strong{Lossless or abort.} A coercion that would damage values — an
\code{integer} dataType truncating fractions or overflowing R's 32-bit range
— aborts with \code{artoo_error_type} before any value is touched. There is
no opt-out: fix the spec (dataType \code{"float"} or \code{"decimal"} keeps
fractions) rather than accept silent damage. The condition carries the
offending rows as data: \code{cnd$variables} is a data frame with columns
\code{variable}, \code{data_type}, \code{n}, and \code{reason} (\code{"truncated"} /
\code{"overflowed"}), so a pipeline can collect every mismatch in one
\code{tryCatch(..., artoo_error_type = function(cnd) cnd$variables)} pass.
The NA-introduction warning (\code{artoo_warning_coercion}) carries the same
frame with \code{reason = "na_introduced"}, and a \code{conformance = "abort"}
failure carries the complete findings frame as \code{cnd$findings}.

\strong{Values are never translated.} Coded variables keep their submission
values (\code{SEX} stays \code{"M"}); codelist translation is its own verb,
\code{\link[=decode_column]{decode_column()}}.
}
\examples{
# ---- Example 1: conform ADSL, then read its metadata ----
#
# The bundled adam_spec describes ADSL; the raw frame is coerced,
# ordered, sorted, and stamped with the CDISC metadata get_meta() reads
# back. Variables the spec declares but this extract never derived are
# reported (not added), readable via conformance().
adsl <- apply_spec(cdisc_adsl, adam_spec, "ADSL")
get_meta(adsl)@dataset$records

# ---- Example 2: extras are kept and reported, or dropped on request ----
#
# By default a column outside the spec rides along (reported by the
# extra_variable finding) and still writes losslessly; extra = "drop"
# trims to the spec, announced and still reported. DM is SDTM, so it
# conforms against the bundled sdtm_spec.
raw <- cdisc_dm
raw$DERIVED <- seq_len(nrow(raw))
dm <- apply_spec(raw, sdtm_spec, "DM")
findings <- conformance(dm)
findings[findings$check == "extra_variable", c("variable", "message")]
trimmed <- apply_spec(raw, sdtm_spec, "DM", extra = "drop")
"DERIVED" \%in\% names(trimmed)

}
\seealso{
\strong{Check:} \code{\link[=check_spec]{check_spec()}} for the findings; \code{\link[=conformance]{conformance()}} to read them
back.

\strong{Translate:} \code{\link[=decode_column]{decode_column()}} for codelist value mapping.

\strong{Metadata:} \code{\link[=get_meta]{get_meta()}} / \code{\link[=set_meta]{set_meta()}} for what the stamp attaches.
}
