% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_accessors.R
\name{spec_codelist}
\alias{spec_codelist}
\title{Terms of one codelist}
\usage{
spec_codelist(spec, codelist_id)
}
\arguments{
\item{spec}{\emph{The specification to read.} \verb{<vport_spec>: required}.}

\item{codelist_id}{\emph{The codelist to return.} \verb{<character(1)>: required}.

\strong{Restriction:} must name a codelist present in the spec's
\code{codelists} slot; an unknown id aborts with \code{vport_error_input}.}
}
\value{
\emph{A data frame of the codelist's terms} (\code{term}, \code{decode},
\code{order}, \code{extended}), one row per term.
}
\description{
Return the controlled-terminology terms and decodes for a single
codelist. Use it to inspect the values a coded variable is allowed to
take before applying the spec.
}
\examples{
# ---- Example 1: the terms behind a coded variable ----
#
# SEX is coded against C66731; spec_codelist() returns the terms and their
# decodes that apply_spec() will enforce or decode.
spec <- vport_spec(cdisc_datasets, cdisc_variables, codelists = cdisc_codelists)
spec_codelist(spec, "C66731")

}
\seealso{
\code{\link[=spec_variables]{spec_variables()}} for which variables reference a codelist.
}
