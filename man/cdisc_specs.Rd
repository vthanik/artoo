% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{cdisc_specs}
\alias{cdisc_specs}
\alias{adam_spec}
\alias{sdtm_spec}
\title{Bundled CDISC specifications (ADaM and SDTM)}
\format{
A validated \code{\link[=artoo_spec]{artoo_spec()}} object; inspect it with
\code{\link[=spec_datasets]{spec_datasets()}}, \code{\link[=spec_variables]{spec_variables()}}, and \code{\link[=spec_standard]{spec_standard()}}.
}
\source{
The CDISC Define-XML 2.1 release example defines (ADaM + SDTM),
pinned by sha256 in \code{data-raw/bundle-spec.R}; data from the PHUSE Test
Data Factory.
}
\usage{
adam_spec

sdtm_spec
}
\description{
Ready-made \code{artoo_spec} objects built from the official CDISC Define-XML
2.1 release examples: \code{adam_spec} (ADaMIG 1.1; datasets ADSL, ADAE) and
\code{sdtm_spec} (SDTMIG 3.1.2; datasets TS, DM, VS, SUPPDM). Every bundled
demo dataset conforms to its spec under
\code{apply_spec(conformance = "abort")} -- the pairing is gated at build
time. The same specs ship as P21 workbooks in
\code{system.file("extdata", "adam-spec.xlsx", package = "artoo")} and
\code{"sdtm-spec.xlsx"}, written by \code{\link[=write_spec]{write_spec()}}.
}
\details{
\strong{Demo adaptations} (each an ADR in \code{data-raw/bundle-spec.R}): the
sponsor-defined codelists (\code{CL.ARM}, \code{CL.ARMCD}, \code{CL.BMICAT}, and the
extensible NCI VS codelists) are marked \code{extended}; \code{VISITNUM} is typed
\code{float} (the pilot data has fractional visit numbers); VS declares the
SDTMIG timepoint variables \code{VSTPT}/\code{VSTPTNUM}, with \code{VSTPTNUM} in the VS
key.
}
\keyword{datasets}
