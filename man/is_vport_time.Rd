% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vport_time.R
\name{is_vport_time}
\alias{is_vport_time}
\title{Test for a vport_time value}
\usage{
is_vport_time(x)
}
\arguments{
\item{x}{\emph{Object to test.} \verb{<any>}.}
}
\value{
\emph{A \verb{<logical(1)>}}: \code{TRUE} when \code{x} is a \code{vport_time}.
}
\description{
Report whether an object is a \code{vport_time} -- vport's clock-time-of-day
class (seconds since midnight, rendered \code{HH:MM:SS}). SAS \code{TIME} columns
read by \code{read_xpt()} and conformed by \code{\link[=apply_spec]{apply_spec()}} arrive as
\code{vport_time}; use this to guard before time-specific handling.
}
\examples{
# ---- Example 1: a time-of-day value is a vport_time ----
#
# The class SAS time variables arrive as after read_xpt()/apply_spec().
is_vport_time(vport_time(c(0, 30600)))

# ---- Example 2: distinguish from Date and POSIXct ----
#
# Only time-of-day columns are vport_time; dates and datetimes are the base
# R classes.
is_vport_time(Sys.Date())

}
\seealso{
\code{\link[=vport_time]{vport_time()}} to build one; \code{\link[=apply_spec]{apply_spec()}} which realizes
temporal columns.
}
