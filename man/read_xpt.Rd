% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codec_xpt.R
\name{read_xpt}
\alias{read_xpt}
\title{Read a dataset from SAS XPORT}
\usage{
read_xpt(path, encoding = NULL, col_select = NULL, n_max = Inf, member = NULL)
}
\arguments{
\item{path}{\emph{Source \code{.xpt} path.} \verb{<character(1)>: required}.}

\item{encoding}{\emph{Force a source charset.} \verb{<character(1)> | NULL}. \code{NULL}
(default) auto-detects (UTF-8 when every character value and label is
valid UTF-8, else Windows-1252). IANA and SAS names both work.}

\item{col_select}{\emph{Variables to read.} \verb{<character> | NULL}. \code{NULL}
(default) reads every column; otherwise a vector of variable names
(matching the names as stored, uppercase for v5). Columns return in file
order, and the \code{artoo_meta} is filtered to match.

\strong{Note:} an unknown name is a \code{artoo_error_input}, never a silent drop.}

\item{n_max}{\emph{Maximum records to read.} \verb{<numeric(1)>: default Inf}. Caps
the row count; the returned \code{artoo_meta} reports the rows actually read.}

\item{member}{\emph{Which member of a multi-member transport file to read.}
\verb{<character(1) | numeric(1)> | NULL}. A transport file can hold several
datasets; pass a member name (case-insensitive) or 1-based index to pick
one. \code{NULL} (default) reads a single-member file directly and aborts on
a multi-member file, pointing at \code{\link[=xpt_members]{xpt_members()}}.

\strong{Tip:} \code{xpt_members(path)} lists what a file holds before you choose.}
}
\value{
\emph{A \verb{<data.frame>}} carrying \code{artoo_meta} (read it with
\code{\link[=get_meta]{get_meta()}}).
}
\description{
Read a SAS Transport (\code{.xpt}) file (v5 or v8) back to a data frame,
restoring the \code{artoo_meta} its NAMESTR records carry and realizing SAS
date/datetime/time variables to R \code{Date} / \code{POSIXct} / \code{hms::hms}. The
ingest end of the I/O layer; a thin wrapper over \code{\link[=read_dataset]{read_dataset()}} with
\code{format = "xpt"}.
}
\details{
The character encoding is auto-detected (UTF-8 if every character value is
valid UTF-8, else Windows-1252) and recorded on the returned
\code{artoo_meta}, so a later \code{\link[=write_xpt]{write_xpt()}} reproduces it; pass \code{encoding} to
override. XPORT cannot record its own encoding, so this detection is a
heuristic. See \code{\link[=write_xpt]{write_xpt()}} for what XPORT can and cannot preserve.
}
\examples{
spec <- artoo_spec(cdisc_datasets, cdisc_variables, codelists = cdisc_codelists)

# ---- Example 1: round-trip a conformed dataset through xpt ----
#
# Write ADSL, read it back; the variable labels and lengths survive.
adsl <- apply_spec(cdisc_adsl, spec, "ADSL", conformance = "off")
path <- tempfile(fileext = ".xpt")
write_xpt(adsl, path)
back <- read_xpt(path)
get_meta(back)@columns$STUDYID$label

# ---- Example 2: pick one member of a multi-member transport file ----
#
# Build a two-member file by concatenating two single-member files (every
# member section is 80-byte padded), then read one dataset out of it.
dm <- apply_spec(cdisc_dm, spec, "DM", conformance = "off")
p_dm <- tempfile(fileext = ".xpt")
write_xpt(dm, p_dm)
multi <- tempfile(fileext = ".xpt")
writeBin(
  c(
    readBin(path, "raw", file.size(path)),
    readBin(p_dm, "raw", file.size(p_dm))[-(1:240)]
  ),
  multi
)
xpt_members(multi)$name
nrow(read_xpt(multi, member = "DM"))

}
\seealso{
\code{\link[=xpt_members]{xpt_members()}} to list a file's members; \code{\link[=write_xpt]{write_xpt()}} for the
inverse; \code{\link[=read_dataset]{read_dataset()}} for the generic dispatcher.
}
