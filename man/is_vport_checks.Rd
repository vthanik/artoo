% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{is_vport_checks}
\alias{is_vport_checks}
\title{Test for a vport_checks control}
\usage{
is_vport_checks(x)
}
\arguments{
\item{x}{\emph{Object to test.} \verb{<any>}.}
}
\value{
\emph{A \verb{<logical(1)>}}: \code{TRUE} when \code{x} is a \code{vport_checks}.
}
\description{
Report whether an object is a \code{vport_checks} control built by
\code{\link[=vport_checks]{vport_checks()}}. Use it to guard a \code{checks} argument before threading it
into \code{\link[=check_spec]{check_spec()}} or \code{\link[=apply_spec]{apply_spec()}}.
}
\examples{
# ---- Example 1: confirm a control before reusing it ----
#
# is_vport_checks() distinguishes a real control from a bare list of flags.
is_vport_checks(vport_checks())
is_vport_checks(list(missing_variable = TRUE))

}
\seealso{
\code{\link[=vport_checks]{vport_checks()}} to build one.
}
