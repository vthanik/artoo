% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_template.R
\name{write_template}
\alias{write_template}
\title{Write a blank Pinnacle 21 workbook to fill in}
\usage{
write_template(path, version = "2.1")
}
\arguments{
\item{path}{\emph{Destination workbook.} \verb{<character(1)>: required}. An
\code{.xlsx} path. Needs the \code{writexl} package.}

\item{version}{\emph{Define-XML version the workbook is for.}
\verb{<character(1)>: default "2.1"}.
\itemize{
\item \code{"2.1"} (default)
\item \code{"2.0"} -- omits the columns 2.1 introduced.
}}
}
\value{
\emph{The output \code{path}, invisibly.} Fill it in, then read it with
\code{\link[=read_spec]{read_spec()}}.
}
\description{
Emit an empty Excel workbook with the sheets and column headers
\code{\link[=read_spec]{read_spec()}} recognises, so a specification can be authored from the
shape the reader wants rather than guessed at. Fill it in, read it back
with \code{\link[=read_spec]{read_spec()}}, and write a define.xml with \code{\link[=write_spec]{write_spec()}}.
}
\details{
\strong{The headers are the reader's own.} Every sheet name and every column
comes from the same maps \code{\link[=read_spec]{read_spec()}} matches against, so a template
cannot offer a column that would be silently ignored, and a column added
to the reader appears here without a second list to maintain.

\strong{A 2.0 template is narrower.} Columns Define-XML 2.1 introduced
(\code{SubClass} and \verb{Has No Data} on Datasets, \code{Source} and \verb{Has No Data} on
Variables) are omitted from a 2.0 template, because a column no 2.0
document can carry is one an author fills for nothing.

\strong{Only Datasets and Variables are required.} Every other sheet may be
left empty; \code{\link[=read_spec]{read_spec()}} omits what it finds nothing in.

\strong{Conditions live on the \code{WhereClauses} sheet.} A value-level row names
a condition by its \code{ID} and the \code{WhereClauses} sheet defines it, one row
per comparison, rows sharing an \code{ID} being ANDed together. This is the
shape the widest range of tools import. \code{\link[=read_spec]{read_spec()}} also reads the
newer shape, which drops that sheet and writes the condition into the
\verb{Where Clause} cell as an expression.

\strong{\code{Standards} is an artoo extension.} Define-XML 2.1 carries a
\code{def:Standards} block that the workbook format has no sheet for, so
artoo offers one rather than lose it.
}
\examples{
# ---- Example 1: a blank workbook, and what it offers ----
#
# The template carries every sheet read_spec() recognises. Datasets and
# Variables are the two it requires; the rest may be left empty.
path <- tempfile(fileext = ".xlsx")
write_template(path)
if (requireNamespace("readxl", quietly = TRUE)) {
  readxl::excel_sheets(path)
}

# ---- Example 2: the 2.0 template omits what 2.0 cannot carry ----
#
# `Source` records who collected a value, which Define-XML 2.1 added. A
# 2.0 workbook has no column for it, so the template does not offer one.
old <- tempfile(fileext = ".xlsx")
write_template(old, version = "2.0")
if (requireNamespace("readxl", quietly = TRUE)) {
  setdiff(
    names(readxl::read_excel(path, sheet = "Variables")),
    names(readxl::read_excel(old, sheet = "Variables"))
  )
}

}
\seealso{
\strong{Fill it in:} \code{\link[=read_spec]{read_spec()}} reads the completed workbook.

\strong{Then write:} \code{\link[=write_spec]{write_spec()}} turns the spec into a define.xml,
\code{\link[=validate_define]{validate_define()}} and \code{\link[=lint_define]{lint_define()}} check the result.
}
