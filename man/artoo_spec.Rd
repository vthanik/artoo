% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_construct.R
\name{artoo_spec}
\alias{artoo_spec}
\title{Construct a CDISC specification}
\usage{
artoo_spec(
  datasets = NULL,
  variables = NULL,
  codelists = NULL,
  study = NULL,
  values = NULL,
  methods = NULL,
  comments = NULL,
  documents = NULL,
  standard = NULL,
  standards = NULL,
  where_clauses = NULL,
  method_expressions = NULL,
  arm_displays = NULL,
  arm_results = NULL,
  dictionaries = NULL
)
}
\arguments{
\item{datasets}{\emph{Dataset-level metadata table.}
\verb{<data.frame>: required}. One row per dataset; must carry a \code{dataset}
column. Optional columns \code{label}, \code{class}, \code{structure}, \code{keys} are
filled with \code{NA} when absent.}

\item{variables}{\emph{Variable-level metadata table.}
\verb{<data.frame>: required}. One row per variable; must carry \code{dataset},
\code{variable}, and \code{data_type}. The \code{data_type} column is canonicalised to
a CDISC \code{dataType} (e.g. \code{"text"} becomes \code{"string"}).

\strong{Requirement:} every \code{dataset} value must appear in \code{datasets}.}

\item{codelists}{\emph{Controlled-terminology terms.}
\verb{<data.frame> | NULL}. Must carry \code{codelist_id} and \code{term} when
supplied.

\strong{Interaction:} every \code{codelist_id} referenced by \code{variables} must
resolve here.}

\item{study}{\emph{Study-level metadata.} \verb{<data.frame> | NULL}. A single row
of named study fields. Well-known fields are canonicalised to
\code{study_name}, \code{study_description}, and \code{protocol_name} (aliases such
as \code{StudyName} or \code{studyid} resolve automatically); other fields pass
through verbatim. A \code{standard} field, when present, is consumed into
\verb{@standard}.}

\item{values}{\emph{Value-level (VLM) metadata.} \verb{<data.frame> | NULL}.}

\item{methods}{\emph{Derivation methods.} \verb{<data.frame> | NULL}. The
Define-XML method definitions variables reference by \code{method_id}; must
carry \code{method_id} when supplied. Completeness (e.g. a referenced
method has a description) is checked by \code{\link[=validate_spec]{validate_spec()}}, not here.}

\item{comments}{\emph{Comment definitions.} \verb{<data.frame> | NULL}. Referenced
by \code{comment_id}; must carry \code{comment_id} when supplied.}

\item{documents}{\emph{Document references.} \verb{<data.frame> | NULL}. Referenced
by \code{document_id}; must carry \code{document_id} when supplied.}

\item{standard}{\emph{The CDISC standard the spec implements.}
\verb{<character(1)> | NULL}. E.g. \code{"ADaMIG 1.1"} or \code{"SDTMIG 3.2"}. When
\code{NULL} (default) it is resolved from \code{datasets$standard} or
\code{study$standard}; absent everywhere, \verb{@standard} is \code{NA}.

\strong{Restriction:} all sources must agree on one value; conflicting
standards abort with \code{artoo_error_spec}.}

\item{standards}{\emph{CDISC standards this spec claims.} \verb{<data.frame> | NULL}.
Must carry \code{standard_id}, \code{name} and \code{version}. Define-XML 2.1 emits these
as a \code{def:Standards} block that datasets and codelists reference by id;
2.0 has room for only one, taken from the row flagged \code{is_primary}.}

\item{where_clauses}{\emph{Structured value-level conditions.}
\verb{<data.frame> | NULL}. Must carry \code{where_clause_id} and \code{comparator}. One
row per check value, because a check value is free text and may itself
contain a comma, so any collapsed form would be lossy.}

\item{method_expressions}{\emph{Formal expressions for derivation methods.}
\verb{<data.frame> | NULL}. Must carry \code{method_id}. A separate table because a
method may carry several expressions in different languages, which extra
rows on \code{methods} could not express without changing what the published
one-row-per-method rule means.}

\item{arm_displays}{\emph{Analysis result displays.} \verb{<data.frame> | NULL}.
Must carry \code{display_id}. Analysis Results Metadata is version-neutral:
the vocabulary is identical for Define-XML 2.0 and 2.1.}

\item{arm_results}{\emph{Analysis results.} \verb{<data.frame> | NULL}. Must carry
\code{display_id} and \code{result_id}. One row per result and analysis dataset,
since each analysis dataset carries its own where-clause reference.}

\item{dictionaries}{\emph{External codelists.} \verb{<data.frame> | NULL}. Must carry
\code{dictionary_id}. A terminology too large to enumerate, named rather than
listed: MedDRA, WHODrug, ISO 3166. Both readers populate it, and a
variable points at one from the same \code{codelist_id} column it would use
for an enumerated list.}
}
\value{
\emph{A validated \code{artoo_spec} object.} Inspect it with
\code{\link[=spec_datasets]{spec_datasets()}} / \code{\link[=spec_variables]{spec_variables()}}, or check it with
\code{\link[=validate_spec]{validate_spec()}}.
}
\description{
Build and validate a \code{artoo_spec} from dataset, variable, and codelist
tables. Each table is coerced to a plain data frame, missing optional
columns are filled with typed \code{NA}s, every variable type is canonicalised
to the CDISC \code{dataType} vocabulary, and cross-slot integrity (dataset and
codelist references) is checked before the object is returned. The spec
is the lingua franca the rest of artoo reads, applies, and serialises.
}
\details{
\strong{Coerce, then validate.} Each table is first coerced to a plain data
frame (a \code{tibble} is accepted and demoted); known columns are cast to
their storage mode and absent optional columns are added as typed \code{NA},
so every downstream reader can trust the schema. Validation runs only
after coercion, on the completed slots.

\strong{Type canonicalisation.} \code{variables$data_type} is mapped through the
closed CDISC \code{dataType} vocabulary (\code{string}, \code{integer}, \code{decimal},
\code{float}, \code{double}, \code{boolean}, \code{date}, \code{datetime}, \code{time}, \code{URI}). Common
SAS / P21 spellings resolve automatically (\code{"text"}, \code{"Char"},
\code{"integer (8)"}, ...); an unrecognised token aborts with
\code{artoo_error_type}.

\strong{Cross-slot integrity.} Construction fails (\code{artoo_error_spec}) if a
variable names a dataset absent from \code{datasets}, or references a
\code{codelist_id} absent from \code{codelists}.

\strong{One spec, one standard.} A \code{artoo_spec} carries exactly one CDISC
standard, stored as the scalar \verb{@standard} property. The constructor
resolves it from the \code{standard} argument, a \code{standard} column in
\code{datasets} (the P21 workbook shape), and a \code{standard} field in \code{study}
(the Define-XML shape) — those columns are consumed, so \verb{@standard} is
the single home. More than one distinct value aborts with
\code{artoo_error_spec}; scope the source to one standard (e.g.
\code{read_spec(path, datasets = ...)}) instead of mixing.

\strong{One study vocabulary.} Well-known study fields are canonicalised to
the CDISC ODM GlobalVariables names, snake_cased: \code{study_name},
\code{study_description}, \code{protocol_name}. Source spellings resolve
automatically (\code{StudyName}, \code{studyid}, ...); fields the vocabulary does
not know pass through verbatim. Aliases that disagree on a value abort
with \code{artoo_error_spec}.
}
\examples{
# ---- Example 1: build a spec from the bundled CDISC-pilot tables ----
#
# `cdisc_sdtm_datasets` and `cdisc_sdtm_variables` hold the CDISC pilot SDTM
# metadata in the shape artoo_spec() expects; the constructor
# canonicalises every type and checks cross-slot integrity.
spec <- artoo_spec(cdisc_sdtm_datasets, cdisc_sdtm_variables, codelists = cdisc_codelists)
spec_datasets(spec)

# ---- Example 2: a focused spec for a single dataset ----
#
# Slice the bundled tables to one dataset (DM) to build a smaller spec.
dm_ds <- cdisc_sdtm_datasets[cdisc_sdtm_datasets$dataset == "DM", ]
dm_var <- cdisc_sdtm_variables[cdisc_sdtm_variables$dataset == "DM", ]
dm_spec <- artoo_spec(dm_ds, dm_var, codelists = cdisc_codelists)
head(spec_variables(dm_spec, "DM")[, c("variable", "label", "data_type")])

}
\seealso{
\strong{Inspect:} \code{\link[=spec_datasets]{spec_datasets()}}, \code{\link[=spec_variables]{spec_variables()}}, \code{\link[=spec_codelists]{spec_codelists()}},
\code{\link[=spec_keys]{spec_keys()}}, \code{\link[=spec_study]{spec_study()}}.

\strong{Check:} \code{\link[=validate_spec]{validate_spec()}}. \strong{Predicate:} \code{\link[=is_artoo_spec]{is_artoo_spec()}}.
}
