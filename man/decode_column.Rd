% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decode_column.R
\name{decode_column}
\alias{decode_column}
\title{Derive or translate a variable through its codelist}
\usage{
decode_column(
  x,
  spec,
  dataset,
  from,
  to = from,
  direction = c("to_decode", "to_code"),
  no_match = c("error", "keep", "na"),
  trim = TRUE,
  ignore_case = FALSE
)
}
\arguments{
\item{x}{\emph{The data frame to extend.} \verb{<data.frame>: required}.}

\item{spec}{\emph{The specification carrying the codelists.} \verb{<artoo_spec>: required}.}

\item{dataset}{\emph{The dataset whose variables apply.} \verb{<character(1)>: required}. Must name a dataset in \code{spec}.}

\item{from}{\emph{The source column.} \verb{<character(1)>: required}. Must be a
column of \code{x}.}

\item{to}{\emph{The destination variable.} \verb{<character(1)>: default }from``.
Defaults to translating in place. A \code{to} declared in the spec gets its
dataType coercion and label; an undeclared `to` is plain character.}

\item{direction}{\emph{Which way to map.} \verb{<character(1)>}. One of:
\itemize{
\item \code{"to_decode"} (default) map codes to their decoded values
(\code{"M"} becomes \code{"Male"}).
\item \code{"to_code"} map decoded values to their submission codes -- the
\code{RACEN}-from-\code{RACE} derivation.
}}

\item{no_match}{\emph{Policy for values absent from the codelist.}
\verb{<character(1)>}. One of \code{"error"} (default), \code{"keep"} (carry the
source value through), or \code{"na"}.}

\item{trim}{\emph{Match after trimming whitespace.} \verb{<logical(1)>: default TRUE}.}

\item{ignore_case}{\emph{Match case-insensitively.} \verb{<logical(1)>: default FALSE}. Case differences are usually genuine CT violations, so this is
opt-in.}
}
\value{
\emph{The data frame \code{x}} with the \code{to} column added (at the end) or
replaced (in place), ready for the next pipeline step.
}
\description{
Map one column's values through a spec codelist -- code to decode or
decode to code -- writing the result to a new variable or in place. This
is the everyday companion to \code{\link[=apply_spec]{apply_spec()}}'s whole-dataset \code{decode}
step: deriving \code{RACEN} from \code{RACE}, recovering submission codes from
decoded values, or decoding a single variable for display, without
re-running the pipeline. When the target variable is declared in the
spec, the result is also coerced to its dataType and labelled, so the
new column lands conformed.
}
\details{
\strong{Which codelist applies.} The codelist attached to \code{to} in the spec
wins (the natural direction for \code{RACEN}-style derivations, where the
numeric variable owns the code/decode pairs); when \code{to} declares none,
\code{from}'s codelist is used. If neither variable references a codelist
the call aborts -- there is nothing to map through.

\strong{Soft matches are reported, never silent.} Values that match only
after trimming whitespace (or case-folding, when \code{ignore_case = TRUE})
still map, with a \code{artoo_warning_codelist} naming the variants --
\code{\link[=check_spec]{check_spec()}} always compares exactly, so clean the source for
submission.
}
\examples{
spec <- artoo_spec(cdisc_sdtm_datasets, cdisc_sdtm_variables, codelists = cdisc_codelists)

# ---- Example 1: decode a coded variable into a display column ----
#
# SEX is coded against C66731; map the codes to their decodes in a new
# column, leaving the submission values untouched.
dm <- decode_column(cdisc_dm, spec, "DM", from = "SEX", to = "SEXDECD")
table(dm$SEX, dm$SEXDECD)

# ---- Example 2: the RACEN pattern, a coded numeric from its decode ----
#
# Declare SEXN as an integer variable owning a numeric codelist, then
# derive it from SEX's decoded values: to_code maps each decode to its
# submission code, and the spec dataType makes the result integer.
vars <- rbind(
  cdisc_sdtm_variables,
  data.frame(
    dataset = "DM", variable = "SEXN", label = "Sex (N)",
    data_type = "integer", length = 8L, order = NA_integer_,
    codelist_id = "SEXN"
  )
)
cls <- rbind(
  cdisc_codelists,
  data.frame(
    codelist_id = "SEXN", term = c("1", "2"),
    decode = c("F", "M"), order = 1:2
  )
)
spec_n <- artoo_spec(cdisc_sdtm_datasets, vars, codelists = cls)
dm_n <- decode_column(cdisc_dm, spec_n, "DM",
  from = "SEX", to = "SEXN", direction = "to_code"
)
str(dm_n$SEXN)

}
\seealso{
\strong{Whole-dataset decode:} \code{\link[=apply_spec]{apply_spec()}} with \verb{decode =}.

\strong{Inspect the terms:} \code{\link[=spec_codelists]{spec_codelists()}}. \strong{Check membership:}
\code{\link[=check_spec]{check_spec()}}.
}
