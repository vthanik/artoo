% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta.R
\name{sync_meta}
\alias{sync_meta}
\title{Re-align metadata with a transformed data frame}
\usage{
sync_meta(x, meta = NULL)
}
\arguments{
\item{x}{\emph{The transformed data frame.} \verb{<data.frame>: required}.}

\item{meta}{\emph{The metadata to reconcile against.} \verb{<vport_meta> | NULL}.
\code{NULL} (default) uses the frame's own \code{metadata_json} attribute.

\strong{Requirement:} when the transform dropped the attribute (base \code{[}
subsetting does), capture \code{get_meta()} before the pipeline and pass it
here; a bare frame with no \code{meta} aborts with \code{vport_error_input}.}
}
\value{
\emph{A \verb{<data.frame>}}: \code{x} re-stamped with the reconciled
\code{vport_meta}. Hand it to any \verb{write_*()} codec.
}
\description{
Re-attach and reconcile a \code{vport_meta} after a transformation that dropped
or reshaped it: the metadata's columns are narrowed and reordered to the
frame's current columns, the record count is refreshed, the keys are
recomputed, and a column the metadata does not describe gets an entry
synthesized from its class and attributes. The one-liner to run after a
dplyr (or base) pipeline, before handing the frame to a \verb{write_*()} codec.
}
\details{
\strong{Why it exists.} Base row subsetting (\code{x[i, ]}) drops the frame's
\code{metadata_json} attribute, and many tidyverse verbs rebuild the frame.
\code{sync_meta()} takes the last-known metadata (the frame's own attribute
when it survived, or an explicit \code{meta}) and makes it agree with the data
again, so the round trip stays lossless without hand-editing.
}
\examples{
spec <- vport_spec(cdisc_datasets, cdisc_variables, codelists = cdisc_codelists)

# ---- Example 1: re-attach after an attribute-dropping subset ----
#
# Base subsetting drops the metadata; capture it first, transform, then
# sync. The metadata narrows to the kept columns and the new row count.
adsl <- apply_spec(cdisc_adsl, spec, "ADSL", on_error = "off")
meta <- get_meta(adsl)
elderly <- adsl[adsl$AGE > 65, c("STUDYID", "USUBJID", "AGE")]
synced <- sync_meta(elderly, meta)
get_meta(synced)@dataset$records

# ---- Example 2: a derived column gains a synthesized entry ----
#
# A new column the metadata does not describe is profiled from its class,
# so the frame still writes losslessly.
adsl$AGEGR9 <- ifelse(adsl$AGE > 65, ">65", "<=65")
synced2 <- sync_meta(adsl)
get_meta(synced2)@columns$AGEGR9$dataType

}
\seealso{
\strong{Read / attach:} \code{\link[=get_meta]{get_meta()}}, \code{\link[=set_meta]{set_meta()}}.

\strong{Produce conformed frames:} \code{\link[=apply_spec]{apply_spec()}}.
}
