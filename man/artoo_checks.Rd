% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{artoo_checks}
\alias{artoo_checks}
\title{Control which conformance checks run}
\usage{
artoo_checks(
  missing_variable = TRUE,
  missing_permissible = TRUE,
  extra_variable = TRUE,
  type_mismatch = TRUE,
  length_overflow = TRUE,
  char_length_limit = TRUE,
  codelist_membership = TRUE,
  codelist_membership_extensible = TRUE,
  label_match = TRUE,
  key_uniqueness = TRUE,
  display_format = TRUE,
  variable_name = TRUE,
  dataset_name = TRUE,
  label_length = TRUE,
  integer_overflow = TRUE,
  integer_fraction = TRUE,
  iso8601_format = TRUE
)
}
\arguments{
\item{missing_variable}{\emph{Flag mandatory spec variables absent from the
data.} \verb{<logical(1)>: default TRUE}.}

\item{missing_permissible}{\emph{Flag permissible (non-mandatory) spec variables
absent from the data.} \verb{<logical(1)>: default TRUE}.}

\item{extra_variable}{\emph{Flag data columns the spec does not declare.}
\verb{<logical(1)>: default TRUE}.}

\item{type_mismatch}{\emph{Flag columns whose storage differs from the spec
dataType.} \verb{<logical(1)>: default TRUE}.}

\item{length_overflow}{\emph{Flag character values longer than the spec length.}
\verb{<logical(1)>: default TRUE}.}

\item{char_length_limit}{\emph{Flag character values longer than the SAS XPORT
v5 / FDA 200-byte limit.} \verb{<logical(1)>: default TRUE}.}

\item{codelist_membership}{\emph{Flag values outside their closed codelist.}
\verb{<logical(1)>: default TRUE}.}

\item{codelist_membership_extensible}{\emph{Flag values outside an extensible
codelist's enumerated terms.} \verb{<logical(1)>: default TRUE}. A codelist
whose \code{extended} flag is \code{TRUE} allows sponsor terms, so a non-member is
a note, never an error; this toggle silences those notes independently
of \code{codelist_membership}.}

\item{label_match}{\emph{Flag a column whose label attribute differs from the
spec label.} \verb{<logical(1)>: default TRUE}.}

\item{key_uniqueness}{\emph{Flag a dataset whose spec key variables do not
uniquely identify its rows.} \verb{<logical(1)>: default TRUE}.}

\item{display_format}{\emph{Flag a date/datetime/time variable whose
displayFormat is not a recognized SAS format of that family.}
\verb{<logical(1)>: default TRUE}.}

\item{variable_name}{\emph{Flag a data column name that violates the XPORT
naming rules.} \verb{<logical(1)>: default TRUE}. Over 8 characters (the v5
limit), over 32 (the v8 limit), or containing anything but ASCII
letters, digits, and underscore.}

\item{dataset_name}{\emph{Flag a dataset name that violates the XPORT naming
rules.} \verb{<logical(1)>: default TRUE}. Same limits as \code{variable_name}.}

\item{label_length}{\emph{Flag a column label attribute over the 40-byte XPORT
v5 / FDA limit.} \verb{<logical(1)>: default TRUE}.}

\item{integer_overflow}{\emph{Flag an integer-typed variable holding values
beyond R's 32-bit integer range.} \verb{<logical(1)>: default TRUE}. Such
values become \code{NA} under coercion, so this is an error, not a warning.}

\item{integer_fraction}{\emph{Flag an integer-typed variable holding fractional
values.} \verb{<logical(1)>: default TRUE}. Coercion would truncate them
(162.6 becomes 162) — a data-integrity event; fix the spec dataType
(\code{float} / \code{decimal}) or the data before conforming.}

\item{iso8601_format}{\emph{Flag a character date/datetime/time variable whose
values are not valid ISO 8601 text.} \verb{<logical(1)>: default TRUE}. A
character column under a temporal dataType is the CDISC \code{--DTC} form;
complete values, right-truncated partials (\code{"1951"}, \code{"1951-12"}), and
SDTMIG hyphen placeholders (\code{"2003---15"}) all pass, while
\code{"12NOV2019"} or an impossible calendar date is flagged.}
}
\value{
\emph{A \verb{<artoo_checks>} control object}. Pass it as the \code{checks}
argument to \code{\link[=check_spec]{check_spec()}}.
}
\description{
Build a reusable control that selects which dimensions \code{\link[=check_spec]{check_spec()}}
evaluates. Construct one per study and thread it through every
check_spec() call so the conformance surface is consistent. Each toggle is validated at construction, so a
mistyped name or value aborts early rather than being silently ignored.
}
\details{
\strong{Selection, not severity.} This control decides which findings are
\emph{produced}; \code{\link[=apply_spec]{apply_spec()}}'s \code{conformance} argument (warn, abort, off)
decides what to \emph{do} with the findings its full-default check raises. A
disabled dimension is skipped entirely, so the findings frame stays
clean.
}
\examples{
# ---- Example 1: the default runs every conformance dimension ----
#
# With no arguments, every conformance dimension is enabled.
artoo_checks()

# ---- Example 2: silence one dimension for a whole study ----
#
# Turn off the length check (e.g. while a spec's lengths are provisional)
# and reuse the control across every dataset.
spec <- artoo_spec(cdisc_sdtm_datasets, cdisc_sdtm_variables, codelists = cdisc_codelists)
ck <- artoo_checks(length_overflow = FALSE)
nrow(check_spec(cdisc_dm, spec, "DM", checks = ck))

}
\seealso{
\code{\link[=check_spec]{check_spec()}}, which consumes it; \code{\link[=apply_spec]{apply_spec()}} for the
findings disposition.
}
